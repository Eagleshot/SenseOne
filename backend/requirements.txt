fastapi>=0.136.3
httpx>=0.28.1
uvicorn[standard]>=0.48.0
python-dotenv>=1.2.2
pydantic>=2.13.4

# SQLite control plane via SQLAlchemy (sqlite3 ships with Python; no driver or
# server needed). Schema is owned by Alembic migrations, run at startup.
SQLAlchemy>=2.0.50
alembic>=1.13
